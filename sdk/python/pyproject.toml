[project]
name = "amplifier-tui-sdk"
version = "0.1.0"
description = "Thin Python wrapper for amplifier-tui's versioned JSONL CLI"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Microsoft MADE:Explorations Team" }]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/amplifier_tui_sdk"]
